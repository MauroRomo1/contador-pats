import { Button } from "react-bootstrap";

const Contador = () => {
  return (
    <section>
      <h3>Contador</h3>
      <h3>20</h3>
      <Button variant="primary">+1</Button>
    </section>
  );
};

export default Contador;
